= form_for @admin_city do |f|
  - if @admin_city.errors.any?
    #error_explanation
      %h2= "#{pluralize(@admin_city.errors.count, "error")} prohibited this admin_city from being saved:"
      %ul
        - @admin_city.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
