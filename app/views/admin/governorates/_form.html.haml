= form_for [:admin, @admin_governorate] do |f|
  - if @admin_governorate.errors.any?
    #error_explanation
      %h2= "#{pluralize(@admin_governorate.errors.count, "error")} prohibited this admin_governorate from being saved:"
      %ul
        - @admin_governorate.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
